name: calisthenics Backend CI/CD

on:
  push:
    branches:
      - main
    paths:
      - "02_cicd/calisthenics-backend/**"
      - ".github/workflows/calisthenics-backend.yml"

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}


      - name: Build Image
        run: |
          docker build \
            -t ${{ secrets.DOCKER_USERNAME }}/calisthenics-backend:latest \
            ./02_cicd/calisthenics-backend

      - name: Push Image
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/calisthenics-backend:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.BACKEND_SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            sudo docker pull ${{ secrets.DOCKER_USERNAME }}/calisthenics-backend:latest

            sudo docker stop calisthenics-backend || true
            sudo docker rm calisthenics-backend || true

            sudo docker run -d \
              --name calisthenics-backend \
              --restart unless-stopped \
              -p 80:8000 \
              ${{ secrets.DOCKER_USERNAME }}/calisthenics-backend:latest